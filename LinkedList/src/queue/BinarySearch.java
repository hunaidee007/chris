package queue;

public class BinarySearch implements ArraySearch{
    @Override
    public boolean search(int[] arr) {
        System.out.println("BinarySearch called..");
        // Provide logic to search using binary method
        return false;
    }
}

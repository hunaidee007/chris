package queue;

public class TestSearch {

    public void search(ArraySearch arraySearch, int arr[]) { // ArraySearch arraySearch = sequentialSearch
                                                             // ArraySearch arraySearch = binarySearch
        arraySearch.search(arr);
    }
}
